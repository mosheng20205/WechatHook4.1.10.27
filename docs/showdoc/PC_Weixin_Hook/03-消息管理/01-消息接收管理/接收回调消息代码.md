# 接收回调消息代码

> Source: [https://www.showdoc.com.cn/PCWeixinHook/11559060626558432](https://www.showdoc.com.cn/PCWeixinHook/11559060626558432)  
> ShowDoc page_id: 11559060626558432

### python 收消息代码示例
```
# encoding:utf-8
from flask import Flask, request, jsonify
from flask_cors import CORS
from flask_httpauth import HTTPTokenAuth
import json
import logging
import qrcode
import pprint

auth = HTTPTokenAuth(scheme='JWT')
app = Flask(__name__)

log = logging.getLogger('werkzeug')
log.setLevel(logging.ERROR)


@app.route('/robot-api/webot/receiveChatBotMsg', methods=["POST"])
def recive_msg():
    if request.method == 'POST':
        ip = request.remote_addr
        print('================= [自定义URL消息接收] ====================')

        # 获取数据
        data = request.json

        if data:
            # 或者使用json.dumps并添加颜色（在支持ANSI颜色的终端中）
            formatted = json.dumps(data, indent=2, ensure_ascii=False)
            print(formatted)
        else:
            pass

        ServerPort = request.json.get('ServerPort') if request.json else None

        return jsonify(code=1, msg="success")

    else:
        return jsonify(code=0, msg="请求方式错误")

if __name__ == '__main__':

    # 下面的代码是消息接收的部分
    app.config['JSON_AS_ASCII'] = False
    CORS(app, supports_credentials=True)

    app.run(host='127.0.0.1', port=8003, debug=True)

```
